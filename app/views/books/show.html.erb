<!DOCTYPE html>
<html>
<head>
  <link rel="preconnect" href="https://fonts.gstatic.com/" crossorigin="" />
  <link
    rel="stylesheet"
    as="style"
    onload="this.rel='stylesheet'"
    href="https://fonts.googleapis.com/css2?display=swap&amp;family=Noto+Sans%3Awght%40400%3B500%3B700%3B900&amp;family=Public+Sans%3Awght%40400%3B500%3B700%3B900"
    />

  <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
</head>


<div class="min-h-screen bg-slate-50" style='font-family: "Public Sans", "Noto Sans", sans-serif;'>
  <header class="flex items-center justify-between whitespace-nowrap border-b border-solid border-b-[#e7edf4] px-10 py-3">
    <div class="flex items-center gap-4 text-[#0d141c]">
      <div class="size-4"></div>
      <h2 class="text-[#0d141c] text-lg font-bold leading-tight tracking-[-0.015em]">HomeLibrary</h2>
    </div>
    <%= link_to 'Back to books', books_path, class: 'inline-block rounded-lg bg-[#e7edf4] text-[#0d141c] px-4 py-2 font-semibold shadow hover:bg-[#d1e3f8] transition' %>
  </header>
  <div class="flex items-center justify-center min-h-[80vh]">
    <div class="w-full max-w-xl bg-white rounded-2xl shadow-lg p-12">
      <h2 class="text-2xl font-bold text-center mb-8"><%= @book.title %></h2>
      <div class="mb-6">
        <p class="mb-2"><span class="font-semibold">Author:</span> <%= @book.author %></p>
        <p class="mb-2"><span class="font-semibold">Genre:</span> <%= @book.genre %></p>
        <p class="mb-2"><span class="font-semibold">Pages:</span> <%= @book.pages %></p>
      </div>
      <div class="flex gap-4 justify-center">
        <%= link_to 'Edit', edit_book_path(@book), class: "flex-1 text-center py-3 rounded-full bg-gray-100 text-gray-700 font-semibold hover:bg-gray-200 transition" %>
        <%= button_to 'Delete', book_path(@book), method: :delete, data: { confirm: 'Are you sure?' }, class: "flex-1 py-3 rounded-full bg-red-500 text-white font-bold hover:bg-red-600 transition" %>
      </div>
    </div>
  </div>
</div>
