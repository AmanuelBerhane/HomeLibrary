<!DOCTYPE html>
<html>
<head>
  <link rel="preconnect" href="https://fonts.gstatic.com/" crossorigin="" />
  <link
    rel="stylesheet"
    as="style"
    onload="this.rel='stylesheet'"
    href="https://fonts.googleapis.com/css2?display=swap&amp;family=Noto+Sans%3Awght%40400%3B500%3B700%3B900&amp;family=Public+Sans%3Awght%40400%3B500%3B700%3B900"
    />

  <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
</head>

  <div class="flex-1 flex flex-col items-center justify-center px-4">
    <div class="w-full max-w-lg">
      <%= form_with(model: @book, local: true) do |form| %>
        <% if @book.errors.any? %>
          <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
            <h3 class="font-bold"><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h3>
            <ul class="mt-2 list-disc list-inside">
              <% @book.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="mb-6">
          <%= form.label :title, class: "block text-gray-700 font-semibold mb-2" %>
          <%= form.text_field :title, placeholder: "Enter book title", class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400 text-base transition disabled:bg-gray-100 #{'border-red-400' if @book.errors[:title].any?}" %>
          <% if @book.errors[:title].any? %>
            <div class="text-red-500 text-sm mt-1"><%= @book.errors[:title].first %></div>
          <% end %>
        </div>

        <div class="mb-6">
          <%= form.label :author, class: "block text-gray-700 font-semibold mb-2" %>
          <%= form.text_field :author, placeholder: "Enter author's name", class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400 text-base transition disabled:bg-gray-100 #{'border-red-400' if @book.errors[:author].any?}" %>
          <% if @book.errors[:author].any? %>
            <div class="text-red-500 text-sm mt-1"><%= @book.errors[:author].first %></div>
          <% end %>
        </div>

        <div class="mb-6">
          <%= form.label :genre, class: "block text-gray-700 font-semibold mb-2" %>
          <%= form.text_field :genre, placeholder: "Enter genre", class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400 text-base transition disabled:bg-gray-100 #{'border-red-400' if @book.errors[:genre].any?}" %>
          <% if @book.errors[:genre].any? %>
            <div class="text-red-500 text-sm mt-1"><%= @book.errors[:genre].first %></div>
          <% end %>
        </div>

        <div class="mb-8">
          <%= form.label :pages, class: "block text-gray-700 font-semibold mb-2" %>
          <%= form.number_field :pages, placeholder: "Enter number of pages", class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400 text-base transition disabled:bg-gray-100 #{'border-red-400' if @book.errors[:pages].any?}" %>
          <% if @book.errors[:pages].any? %>
            <div class="text-red-500 text-sm mt-1"><%= @book.errors[:pages].first %></div>
          <% end %>
        </div>

        <div class="flex gap-4 justify-center">
          <%= link_to "Cancel", books_path, class: "flex-1 text-center py-3 rounded-full bg-gray-100 text-gray-700 font-semibold hover:bg-gray-200 transition" %>
          <%= form.submit @book.persisted? ? "Update Book" : "Save Book", class: "flex-1 py-3 rounded-full bg-blue-500 text-white font-bold hover:bg-blue-600 transition" %>
        </div>
      <% end %>
    </div>
  </div>
</div>